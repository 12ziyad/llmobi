<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".LLMobiApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.LLMobi">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LLMobi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          One activity per model, faked with task affinity.
          documentLaunchMode + a unique taskAffinity per shortcut makes each
          model show up as its own card in Recents, which is what sells the
          "separate app" illusion.
        -->
        <activity
            android:name=".ChatActivity"
            android:exported="true"
            android:documentLaunchMode="intoExisting"
            android:excludeFromRecents="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LLMobi">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="llmobi" android:host="chat" />
            </intent-filter>
        </activity>

        <!--
          WorkManager declares this service with no foregroundServiceType, and on
          Android 14+ the type passed to startForeground must be a subset of what
          the manifest declares - otherwise the service throws on the MAIN thread
          and takes the whole process with it, which no try/catch in a worker can
          reach. Merging the type in here is the documented fix.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>
</manifest>
