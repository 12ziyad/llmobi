Admin: drop the password

The dashboard is read-only and shows only aggregate counts - the downloads
table stores a timestamp, a country and a platform, never an IP or any
identifier - so the password was guarding numbers rather than anything
private, at the cost of a login prompt every time it was opened on a phone.

Open now, with a noindex header so it is not indexed and stumbled onto. The
counting route and the catalog are untouched, and nothing in the worker
accepts a write from the outside.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
